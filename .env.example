# BrsApi free key. PoolamCo calls the comprehensive Gold_Currency endpoint once
# per full app load and only again when the user explicitly presses Refresh.
# The same response is reused across every route and contains gold/currency/crypto.
BRS_API_KEY=
