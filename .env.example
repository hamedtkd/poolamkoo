# BrsApi server-side key. Poolamkoo uses BrsApi for public market rates and
# for exchange data when another market source is temporarily unavailable.
BRS_API_KEY=

# TSETMC is queried through Poolamkoo server routes and does not require an API key.
# The browser never calls TSETMC directly with personal financial data.

# Tindex server-side API token. Tindex is one of Poolamkoo's financial data sources.
# Keep this token private. Do not expose it with a NEXT_PUBLIC_ prefix.
TINDEX_API_TOKEN=

# Background Web Push is intentionally paused in the public zero-cost roadmap.
# Normal deployments do not need Redis, VAPID keys, a cron service, or this flag.
# The preserved experiment can be reviewed in docs/backlog/background-push.md.
# NEXT_PUBLIC_EXPERIMENTAL_BACKGROUND_PUSH=0

# Optional Cloudflare Web Analytics site token. This token is public by design because
# it is embedded in the browser beacon. Leave empty to ship with zero analytics.
# Poolamkoo loads the beacon only in production and never sends custom financial events.
NEXT_PUBLIC_CLOUDFLARE_WEB_ANALYTICS_TOKEN=

# Canonical public URL used by metadata, robots.txt, sitemap.xml and the Origin/Referer headers
# sent by server-side Tindex requests so the sponsored API plan can be bound to this domain.
# The default is https://poolamkoo.vercel.app. Set this when moving to a custom domain.
NEXT_PUBLIC_SITE_URL=https://poolamkoo.vercel.app
