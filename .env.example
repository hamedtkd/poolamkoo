# BrsApi: gold, currency, and crypto. Poolamco calls the comprehensive endpoint
# once per full app load and again only on explicit refresh.
BRS_API_KEY=

# Tindex: Tehran Stock Exchange stocks/ETFs plus fallback for core public market quotes.
# Create a developer token at tindex.app and keep it server-side only.
# BrsApi stays primary; Tindex fallback is called only when core BrsApi quotes are missing.
TINDEX_API_TOKEN=
